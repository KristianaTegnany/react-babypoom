import id from '../../../lib/types';

export const
  BPOOM                 = id(),
  CURRENT_STEP          = id(),
  STEP_INDEX            = id(),
  AVAILABLE_STEPS       = id(),
  SAVE_VISITORBOOK_MSG  = id();