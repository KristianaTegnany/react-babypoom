// Locales & Intl
import { addLocaleData } from 'react-intl'

// STRING REPLACEMENT PLUGIN
//===========================
// import all available locales

//replace: import ${defaultLoc}Data from 'react-intl/locale-data/${defaultLoc}'; :end

//replace: import ${loc}Data from 'react-intl/locale-data/${loc}'; :end
//replace: import ${loc} from '../../config/locales/${loc}.json'; :end

//replace: [${defaultLoc}Data, ${allData}].forEach(function(loc) { addLocaleData(loc); }); :end
//replace: export var messages = { ${all} }; :end
